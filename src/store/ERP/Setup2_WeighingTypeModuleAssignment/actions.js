import axios from 'axios'

export function trigger_get_weight_type_module_get_weight_type_module ({ commit,state }, payload) {

  var object_pass = {
      "url": `weight_type_module/get_weight_type_module/`,  //api request url
      "update_path": 'update_weight_type_module_get_weight_type_module', //function name use in store mutations.js to mutate state to store data in variable
      "type": 'POST', //type of api request
      "headers": {},  // declare to define what data type used such as JSON or multipart/form-data
      "params": payload.payload.params,  //params will auto convert as url request on url in ?parameter=first&parameter_second=second
      "body": payload.payload.pass_json //field that used by api to create data
  };

  return payload.app.$actions({ commit,state }, object_pass);

}

export function trigger_create_weight_type_module_get_weight_type_module ({ commit,state }, payload) {

  var object_pass = {
      "url": `weight_type_module/create_weight_type_module/`,  //api request url
      "update_path": 'update_weight_type_module_get_weight_type_module', //function name use in store mutations.js to mutate state to store data in variable
      "type": 'POST', //type of api request
      "headers": {},  // declare to define what data type used such as JSON or multipart/form-data
      "params": payload.payload.params,  //params will auto convert as url request on url in ?parameter=first&parameter_second=second
      "body": payload.payload.pass_json //field that used by api to create data
  };

  return payload.app.$actions({ commit,state }, object_pass);

}


export function trigger_patch_weight_type_module_get_weight_type_module ({ commit,state }, payload) {

  var object_pass = {
      "url": `weight_type_module/update_weight_type_module/`,  //api request url
      "update_path": 'update_weight_type_module_get_weight_type_module', //function name use in store mutations.js to mutate state to store data in variable
      "type": 'POST', //type of api request
      "headers": {},  // declare to define what data type used such as JSON or multipart/form-data
      "params": payload.payload.params,  //params will auto convert as url request on url in ?parameter=first&parameter_second=second
      "body": payload.payload.pass_json //field that used by api to create data
  };

  return payload.app.$actions({ commit,state }, object_pass);

}

export function trigger_delete_weight_type_module_get_weight_type_module ({ commit,state }, payload) {

  var object_pass = {
      "url": `weight_type_module/delete_weight_type_module/`,  //api request url
      "update_path": 'update_weight_type_module_get_weight_type_module', //function name use in store mutations.js to mutate state to store data in variable
      "type": 'POST', //type of api request
      "headers": {},  // declare to define what data type used such as JSON or multipart/form-data
      "params": payload.payload.params,  //params will auto convert as url request on url in ?parameter=first&parameter_second=second
      "body": payload.payload.pass_json //field that used by api to create data
  };

  return payload.app.$actions({ commit,state }, object_pass);

}

export function trigger_get_weight_type_module_get_weight_type_module_c ({ commit,state }, payload) {

  var object_pass = {
      "url": `weight_type_module/get_weight_type_module_c/`,  //api request url
      "update_path": 'update_weight_type_module_get_weight_type_module_c', //function name use in store mutations.js to mutate state to store data in variable
      "type": 'POST', //type of api request
      "headers": {},  // declare to define what data type used such as JSON or multipart/form-data
      "params": payload.payload.params,  //params will auto convert as url request on url in ?parameter=first&parameter_second=second
      "body": payload.payload.pass_json //field that used by api to create data
  };

  return payload.app.$actions({ commit,state }, object_pass);

}

export function trigger_create_weight_type_module_get_weight_type_module_c ({ commit,state }, payload) {

  var object_pass = {
      "url": `weight_type_module/create_weight_type_module_c/`,  //api request url
      "update_path": 'update_weight_type_module_get_weight_type_module_c', //function name use in store mutations.js to mutate state to store data in variable
      "type": 'POST', //type of api request
      "headers": {},  // declare to define what data type used such as JSON or multipart/form-data
      "params": payload.payload.params,  //params will auto convert as url request on url in ?parameter=first&parameter_second=second
      "body": payload.payload.pass_json //field that used by api to create data
  };

  return payload.app.$actions({ commit,state }, object_pass);

}

export function trigger_patch_weight_type_module_get_weight_type_module_c ({ commit,state }, payload) {

  var object_pass = {
      "url": `weight_type_module/update_weight_type_module_c/`,  //api request url
      "update_path": 'update_weight_type_module_get_weight_type_module_c', //function name use in store mutations.js to mutate state to store data in variable
      "type": 'POST', //type of api request
      "headers": {},  // declare to define what data type used such as JSON or multipart/form-data
      "params": payload.payload.params,  //params will auto convert as url request on url in ?parameter=first&parameter_second=second
      "body": payload.payload.pass_json //field that used by api to create data
  };

  return payload.app.$actions({ commit,state }, object_pass);

}

export function trigger_delete_weight_type_module_get_weight_type_module_c ({ commit,state }, payload) {

  var object_pass = {
      "url": `weight_type_module/delete_weight_type_module_c/`,  //api request url
      "update_path": 'update_weight_type_module_get_weight_type_module_c', //function name use in store mutations.js to mutate state to store data in variable
      "type": 'POST', //type of api request
      "headers": {},  // declare to define what data type used such as JSON or multipart/form-data
      "params": payload.payload.params,  //params will auto convert as url request on url in ?parameter=first&parameter_second=second
      "body": payload.payload.pass_json //field that used by api to create data
  };

  return payload.app.$actions({ commit,state }, object_pass);

}

export function trigger_get_weight_parameter_get_weight_parameter ({ commit,state }, payload) {

  var object_pass = {
      "url": `weight_parameter/get_weight_parameter/`,  //api request url
      "update_path": 'update_weight_parameter_get_weight_parameter', //function name use in store mutations.js to mutate state to store data in variable
      "type": 'POST', //type of api request
      "headers": {},  // declare to define what data type used such as JSON or multipart/form-data
      "params": payload.payload.params,  //params will auto convert as url request on url in ?parameter=first&parameter_second=second
      "body": payload.payload.pass_json //field that used by api to create data
  };

  return payload.app.$actions({ commit,state }, object_pass);

}


